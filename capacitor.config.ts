import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.spine.congress2027',
  appName: '39. Bilimsel Kongre',
  webDir: 'dist'
};

export default config;
